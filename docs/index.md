# Living Architecture Docs

This site is **maintained automatically** by the OpenCode `docs` agent. When
source code changes, the agent updates these pages in the same pass — so what
you read here always reflects what the code actually does.

- **Overview & system map:** see [Architecture](architecture/overview.md)
- **Components:** see the Orders section in the nav

> This page itself is editable by the docs agent. Try changing a service and
> watch the relevant page update.
